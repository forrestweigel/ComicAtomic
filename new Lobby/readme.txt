make sure all files are in WWW wamp folder

open CMD and change directory to www/src folder

run command php socket-server.php to start server

open local host to run webpage

*composer package must be installed
is.bat can be ran inside root folder with CMD to install composer


//to have code update in all clients fix the line where the client excludes itself from its own message